'use client';

import React from 'react';

const AboutPage: React.FC = () => {
    return (
        <div>
            <h1>About Our Application</h1>
            <p>This is a multi-page application using React and React Router.</p>
        </div>
    );
};

export default AboutPage;
